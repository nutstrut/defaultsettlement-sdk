{
  "name": "defaultsettlement-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Default Settlement SDK monorepo. Phase 1: @defaultsettlement/sar-402 — Settlement Attestation Receipts for x402 resource servers.",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
